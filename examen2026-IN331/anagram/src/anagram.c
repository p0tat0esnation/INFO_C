#include <stdio.h>
#include <stdlib.h>
#include <string.h>

#include "functions.h"

/* Find and print anagrams of a given word from a dictionary file.
    @param word The word to find anagrams for
    @param dict_file_path Path to the dictionary file
    @param case_sensitive 1 for case sensitive, 0 for case insensitive (default is case sensitive if unset)
    @return 0 if at least one anagram is found, 1 otherwise
*/
int main(int argc, const char *argv[])
{
    //    TODO: Implement the main function to handle command line arguments,
    //    call find_anagram_in_dict, and return appropriate exit code.

    return 0;
}