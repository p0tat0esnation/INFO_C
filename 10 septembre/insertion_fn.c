#include <stdio.h>



int insertion(float tab[10], int nb_element, int val)
{
    printf("numcase %d nbnotes %f\n", 0, tab[0]);
    return 0;
}

int main() {
    float note_a_inserer[10] = {0}; // Example initialization
    insertion(note_a_inserer, 4, 0);
    return 0;
}
