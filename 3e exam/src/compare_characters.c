#include <stdio.h>
#include <stdlib.h>
#include <string.h>

#include "functions.h"

/* Compare deux caractères, avec gestion optionnelle de la casse.
    @param first_char Premier caractère
    @param second_char Deuxième caractère
    @param case_sensitive 1 si sensible à la casse, 0 sinon
    @returns 1 si égaux, 0 sinon
*/
int compare_characters(const char first_char, char second_char, int case_sensitive)
{
    // TODO: Implémenter la comparaison des caractères.
    (void)first_char;
    (void)second_char;
    (void)case_sensitive;
    return 0;
}