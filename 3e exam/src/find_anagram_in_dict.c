#include <stdio.h>
#include <stdlib.h>
#include <string.h>

#include "functions.h"

/* Cherche les anagrammes d'un mot dans un fichier dictionnaire et les affiche via printf.
    @param file_path Chemin du fichier dictionnaire
    @param word Mot recherché
    @param case_sensitive 1 si sensible à la casse, 0 sinon
    @return nombre d'anagrammes trouvés
*/
int find_anagram_in_dict(const char *file_path, const char *word, int case_sensitive)
{
    // TODO: Implémenter la recherche dans le dictionnaire.
    (void)file_path;
    (void)word;
    (void)case_sensitive;
    return 0;
}